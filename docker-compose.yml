services:
  leash:
    image: leash:latest
    build: .
    command: serve --policy /policies/ --runs-dir /runs
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - ./policies:/policies
      - ./runs:/runs
    restart: unless-stopped
